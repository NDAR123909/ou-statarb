__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.DS_Store
*.csv
*.npz
# the public track record is never ignored — it IS the product
!track_record/**
# cron logs belong in /var/log; this is a guard against one landing here and
# being swept up by `git add -A`
*.log
deploy/ltp_state.json
deploy/ltp_hwm.json
deploy/ltp_ledger.jsonl

# the RapidX CLI's own state dir on the droplet: automation-session records
# and a 745 KB symbol-rules cache. Operational, regenerable, mode 0600, and
# nothing anyone should publish. Untracked-but-not-ignored until 2026-09-21,
# which is one `git add -A` away from pushing account state to GitHub.
.rapidx/

# derived: rebuild with deploy/reasoning_log.py --archive
track_record/phase1_submission.tar.gz
